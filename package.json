{
  "name": "book-finder-library-manager",
  "version": "1.0.0",
  "description": "Book Finder & Personal Library Manager with React and JSON Server",
  "scripts": {
    "frontend": "cd frontend && npm run dev",
    "backend": "node server.js",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "json-server": "^0.17.4"
  }
}
